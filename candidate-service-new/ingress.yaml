apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test-ingress
spec:
  rules:
    - host: "mohnish57920.com"
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: candidate-service
                port:
                  number: 8080

