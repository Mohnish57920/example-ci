apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test-ingress
spec:
  rules:
    - host: "example.com"
      http:
        paths:
          - pathType: prefix
            path: /
            backend:
              service:
                name: candidate-service
                port:
                  number: 8080

